@model PortfolioPanel.Models.TagIndexViewModel


@{
    ViewBag.Title = Model.ViewTitle;
}

<div class="card">
    <div class="card-header bg-gradient-info align-middle">
        <h4 class="text-light float-left m-0">@Model.ViewTitle</h4>
        @Html.ActionLink("Create New Tag", "Create", "Tag", new { }, new { @class = "btn btn-light btn-sm float-right" })
    </div>
    <div class="card-body">
        <table class="table table-striped table-bordered bg-white text-center" id="posts">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Title</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Tags)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.Title</td>
                        <td>
                            @if (item.Active)
                            {
                                <i class="fas fa-check-circle text-success"></i>
                            }
                            else
                            {
                                <i class="fas fa-times-circle text-danger"></i>
                            }
                            @if (item.Deleted)
                            {
                                <i class="fas fa-trash text-danger"></i>
                            }
                            else
                            {
                                <i class="fas fa-trash text-success"></i>
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

